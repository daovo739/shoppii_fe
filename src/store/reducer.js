const initState = {
    filters: {
        categories: [],
        locations: [],
    },
}

const reducer = (state = initState, action) => {
    switch (action.type) {
        // case value:

        //     break;

        default:
            break
    }
}

export default reducer
export { initState }
